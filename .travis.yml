dist: "xenial"
language: "c"
os: "linux"

before_install:
    - "sudo apt-get install gfortran"
    - "git clone https://github.com/bats-core/bats-core.git && cd bats-core && sudo ./install.sh /usr/local && cd .."

script:
    - "cd latex/pr10/homework/01 && make && bats tests.bats"
    - "cd latex/pr10/programme && make && bats tests.bats"
